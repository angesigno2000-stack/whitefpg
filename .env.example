# Hash bcrypt della password admin. Generalo con: node scripts/hash-password.js "tua-password"
ADMIN_PASSWORD_HASH=

# Stringa segreta casuale per firmare le sessioni admin. Generala con:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=
